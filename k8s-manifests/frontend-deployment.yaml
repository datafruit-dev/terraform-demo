apiVersion: apps/v1
kind: Deployment
metadata:
  name: image-editor-frontend
  namespace: image-editor
  labels:
    app: image-editor-frontend
    component: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: image-editor-frontend
  template:
    metadata:
      labels:
        app: image-editor-frontend
        component: frontend
    spec:
      containers:
      - name: frontend
        image: 642375200181.dkr.ecr.us-east-1.amazonaws.com/image-editor-frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
          name: http
        env:
        - name: PORT
          value: "3000"
        - name: NEXT_PUBLIC_API_URL
          value: "http://image-editor-backend.image-editor.svc.cluster.local:8080"
        - name: ENVIRONMENT
          value: "production"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: image-editor-frontend
  namespace: image-editor
  labels:
    app: image-editor-frontend
spec:
  type: NodePort
  selector:
    app: image-editor-frontend
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http